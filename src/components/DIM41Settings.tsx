/** @jsx jsx */
import { jsx } from "@emotion/react";
import React from "react";

type DIM41SettingsProps = {
  foundryApplication: Application,
};

export const DIM41Settings: React.FC<DIM41SettingsProps> = ({
  foundryApplication,
}) => {
  return (
    <div>settings</div>
  );
};
